add_executable(basename basename.c)

INSTALL(TARGETS basename DESTINATION ${CMAKE_INSTALL_BINDIR})

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/basename.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/basename.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)