add_executable(dirname dirname.c)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/dirname.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS dirname DESTINATION ${CMAKE_INSTALL_BINDIR})

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/dirname.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)