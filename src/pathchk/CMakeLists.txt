include(${CMAKE_CURRENT_SOURCE_DIR}/../../cmake/manpage.cmake)


add_executable(pathchk pathchk.c)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/pathchk.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS pathchk DESTINATION ${CMAKE_INSTALL_BINDIR})

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/pathchk.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)
