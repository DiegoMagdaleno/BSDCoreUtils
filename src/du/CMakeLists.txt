add_executable(du du.c)

target_link_libraries(du PRIVATE compat)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/du.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/du.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)