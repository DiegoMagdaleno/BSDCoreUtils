include(${CMAKE_CURRENT_SOURCE_DIR}/../../cmake/manpage.cmake)

add_executable(wc wc.c)

target_link_libraries(wc PRIVATE compat)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/wc.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS wc DESTINATION ${CMAKE_INSTALL_BINDIR})

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/wc.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)
