add_executable(tee tee.c)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/tee.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS tee DESTINATION ${CMAKE_INSTALL_BINDIR})


INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/tee.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)
