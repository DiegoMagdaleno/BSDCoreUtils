include(${CMAKE_CURRENT_SOURCE_DIR}/../../cmake/manpage.cmake)

add_executable(tty tty.c)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/tty.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS tty DESTINATION ${CMAKE_INSTALL_BINDIR})

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/tty.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)
