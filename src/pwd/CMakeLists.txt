include(${CMAKE_CURRENT_SOURCE_DIR}/../../cmake/manpage.cmake)

add_executable(pwd pwd.c)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/pwd.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS pwd DESTINATION ${CMAKE_INSTALL_BINDIR})

INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/pwd.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)
