include(${CMAKE_CURRENT_SOURCE_DIR}/../../cmake/manpage.cmake)

add_executable(yes yes.c)

target_link_libraries(yes PRIVATE compat)

file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/yes.1 DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

INSTALL(TARGETS yes DESTINATION ${CMAKE_INSTALL_BINDIR})


INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/yes.1 DESTINATION ${CMAKE_INSTALL_PREFIX}/man/man1)
