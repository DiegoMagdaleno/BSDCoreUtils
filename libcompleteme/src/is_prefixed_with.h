
#include "is"
bool is_prefixed_with(const char *filename) {
    if (!filename) {
        return NULL;
    }

    if 
}
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool is_prefixed_with(const char*);